name: FryTrainerGate Controller image

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - dev
      - "feat/**"
    paths:
      - "frytrainergate/controller/**"
      - "frytrainergate/shared/**"
      - ".github/workflows/frytrainergate-controller.yml"

permissions:
  contents: read
  packages: write

jobs:
  controller:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Create safe branch tag
        id: metadata
        shell: bash
        run: |
          branch_tag="branch-$(printf '%s' "$GITHUB_REF_NAME" | tr '/' '-')"
          echo "branch_tag=$branch_tag" >> "$GITHUB_OUTPUT"
      - name: Build and publish Controller
        uses: docker/build-push-action@v7
        with:
          context: .
          file: frytrainergate/controller/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/fry2412/frigate-frytrainergate:sha-${{ github.sha }}
            ghcr.io/fry2412/frigate-frytrainergate:feature-${{ github.sha }}
            ghcr.io/fry2412/frigate-frytrainergate:${{ steps.metadata.outputs.branch_tag }}
          cache-from: type=registry,ref=ghcr.io/fry2412/frigate-frytrainergate:buildcache
          cache-to: type=registry,ref=ghcr.io/fry2412/frigate-frytrainergate:buildcache,mode=max
